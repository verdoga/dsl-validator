"use strict";
const progress=document.querySelector("[data-progress]");
if(progress){let event="";const poll=async()=>{try{const response=await fetch("/api/progress",{headers:{Accept:"application/json"}});if(!response.ok)throw new Error("HTTP "+response.status);const state=await response.json();progress.textContent=`Обработано ${state.processed} из ${state.total}; осталось ${state.remaining}; текущий файл: ${state.current||"нет"}`;if(state.event!==event){document.querySelector("[role=status]").textContent=state.event;event=state.event}if(!state.finished)setTimeout(poll,700)}catch(error){if(event!=="Ошибка обновления прогресса"){event="Ошибка обновления прогресса";document.querySelector("[role=status]").textContent=event}setTimeout(poll,1500)}};poll()}
